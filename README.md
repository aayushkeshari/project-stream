# project-stream

End-to-end data engineering project using **Snowflake + Python** with real data.

## Pipeline Overview
Raw Data → Snowflake (RAW) → SQL Transformations → Analytics Tables → Data Quality Checks
